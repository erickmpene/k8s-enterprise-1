apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: site-1.kingakati.com-issuer
  namespace: cert-manager
spec:
  acme:
    email: risty.beatmaker@gmail.com
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: site-1.kingakati.com-issuer-account-key
    solvers:
      - http01:
          ingress:
            class: nginx
