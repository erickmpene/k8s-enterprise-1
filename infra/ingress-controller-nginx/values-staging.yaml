namespaceOverride: ingress-nginx
controller:
  replicaCount: 2
  ingressClassResource:
    name: nginx
    enabled: true
  ingressClass: nginx
  service:
    type: NodePort  # ou NodePort si bare-metal sans MetalLB
